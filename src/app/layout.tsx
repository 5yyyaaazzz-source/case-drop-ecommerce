import type { Metadata } from 'next';
import './globals.css';
import Navbar from '@/components/layout/Navbar';
import Footer from '@/components/layout/Footer';

export const metadata: Metadata = {
  title: 'CASE DROP - Premium Phone Cases | DROP YOUR STYLE',
  description: 'Premium, minimal phone cases designed for people who don\'t do boring. Shop our latest drops, create custom cases, and make your phone yours.',
  keywords: 'phone cases, custom cases, premium cases, streetwear, phone accessories',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <head>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
      </head>
      <body>
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
